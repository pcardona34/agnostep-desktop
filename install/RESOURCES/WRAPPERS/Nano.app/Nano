#!/bin/bash

APP=nano
APPNAME=Nano

if [ $# -eq 2 ] ; then
	ARG="$2"
	TITLE="$APPNAME - $ARG"
else
	ARG=""
	TITLE="$APPNAME"
fi

xterm -T "$TITLE" -e "transset -a --dec 0.05;$APP $ARG" &
PID=$!
pidwait $PID
